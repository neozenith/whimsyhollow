# Copy to .env for local runs. Defaults give a fully local dev loop.

ENVIRONMENT=local

# --- Misc ---
LOG_LEVEL=DEBUG
PORT=8080

# Built React SPA dir served by FastAPI. Defaults to ../frontend/dist (repo-relative).
# In the container the SPA is copied to /app/frontend_dist:
# STATIC_DIR=/app/frontend_dist
